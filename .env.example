# API Keys — only the one matching your provider is required.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=
NVIDIA_API_KEY=
CEREBRAS_API_KEY=
GROQ_API_KEY=

# Custom OpenAI-compatible endpoints (added with /endpoint).
# One var per endpoint, named after its id in ~/.portscope/endpoints.json.
# PORTSCOPE_CUSTOM_MY_GATEWAY_TOKEN=
